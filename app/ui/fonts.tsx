import { Oxygen, Lusitana  } from "next/font/google";

export const allura = Oxygen({
    subsets: ["latin"],
    weight: "300"
});

export const secoundary = Lusitana({
    subsets: ["latin"],
    weight: "400",
});